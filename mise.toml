# Backstage supports even LTS Node only (20/22/24). The global mise pin is 26,
# which create-app rejects. This scopes Node 22 to the lab directory so
# 'make portal' works without touching your global setup.
[tools]
node = "22"
