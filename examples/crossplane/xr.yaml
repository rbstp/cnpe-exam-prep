# The self-service request a developer would make (or Backstage would generate).
apiVersion: platform.lab.local/v1alpha1
kind: AppEnvironment
metadata:
  name: team-c-dev
  namespace: default
spec:
  team: team-c
  cpuQuota: "4"
  memoryQuota: 8Gi
