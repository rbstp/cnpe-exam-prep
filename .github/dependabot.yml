version: 2
updates:
  # The workflows are the only committed dependency manifests: the npm packages
  # CI uses are pinned inline with 'npm install --no-save', and the Backstage
  # app under portal/ is generated locally and gitignored.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
